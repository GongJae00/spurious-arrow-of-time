# Figure Plan

Figures must be generated from code and trace back to logged diagnostics or
metrics. Do not manually assemble result figures.

## Figure 1: Problem Schematic

Compact schematic showing:

```text
hidden source -> irreversible core diffusion -> label
independent nuisance arrow -> train shortcut -> OOD reversal/removal
mixed observation
counterfactual nuisance replacement
```

Do not use a tall component grid as the main schematic.

Status:

```text
Needs final manuscript-quality compact version.
Raw component grids may remain supplementary only.
Generated output name:
  problem_schematic.png
```

## Figure 2: Benchmark Gates

Shows:

```text
final-frame ambiguity
full-sequence recoverability
nuisance IID/OOD collapse
mixed feature-probe gap
```

Status:

```text
Must be generated from diagnostics.json.
Must label feature probes as diagnostics, not neural model results.
```

## Figure 3: Main Neural Results

Shows:

```text
IID and OOD accuracy by method
mean across seeds
error bars
chance reference line
```

Status:

```text
Generated by src/eval/main_results.py from metrics.jsonl.
Needs visual inspection before manuscript use.
```

## Figure 4: Sweep

Shows either:

```text
nuisance strength vs OOD gap
or OOD mode vs OOD gap
or scenario heatmap covering OOD mode, nuisance strength, core difficulty, and
negative controls
```

Status:

```text
Needed for paper if sweep evidence is promoted beyond diagnostic.
If only sweep_pilot exists, caption must say runtime-limited diagnostic.
Generated output names:
  scenario_ood_gap_heatmap.png
  scenario_ood_accuracy_heatmap.png
```

## Visual QA

```text
text legible at paper column width
no overlapping tick labels
consistent colors
chance/reference lines visible
error bars explained
caption does not imply unsupported success
all plotted values trace to metrics logs
```
