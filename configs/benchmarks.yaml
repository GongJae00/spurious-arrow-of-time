# Table 3 named constructions, plus Simple OE, sinusoid, and real-video transfer.
trail_fl:
  nuisance_trail_decay: 0.78
simple_oe:
  nuisance_trail_decay: 0.0
oe_strict:
  overlay: oe_strict
  nuisance_trail_decay: 0.0
set_mf:
  overlay: set_mf
  nuisance_trail_decay: 0.0
mf_core:
  nuisance_trail_decay: 0.0
  diffusion_start_step: 8
  diffusion_steps_between_frames: 2
  core_noise_std: 0.045
  core_noise_growth_power: 0.0
  observation_noise_std: 0.01
oe_core:
  nuisance_trail_decay: 0.0
  core_process: directional_pulse
oe_core_equalized:
  nuisance_trail_decay: 0.0
  core_process: directional_pulse
  core_direction_flip_prob: 0.03
sinusoid:
  overlay: sinusoid
  nuisance_trail_decay: 0.0
real_video:
  nuisance_motion: real_video
  real_video_cache: data/real_video/cache_g16_L8_s5.npz
real_video_blur:
  nuisance_motion: real_video
  real_video_cache: data/real_video/cache_g16_L8_s5.npz
  real_video_blur_sigma: 1.5
  nuisance_scale: 1.2
